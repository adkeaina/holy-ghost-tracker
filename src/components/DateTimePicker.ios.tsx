import DateTimePickerComponent, {
  IOSNativeProps,
} from "@react-native-community/datetimepicker";

export default function DateTimePicker(props: IOSNativeProps) {
  return <DateTimePickerComponent {...props} />;
}
